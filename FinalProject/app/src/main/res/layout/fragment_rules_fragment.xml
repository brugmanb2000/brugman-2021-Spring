<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rules_frag_frame"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ItemActivity.Fragments.rules_fragment">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="To use this app, either host a session or join a session. The host will give out a session ID to use when joining. From there, add items of your choice to vote on. The results will be shown after a round of voting. Have fun =)"
        android:textAlignment="center"
        android:textSize="18sp" />

</FrameLayout>