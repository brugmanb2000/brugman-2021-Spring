<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hw11">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HW11">
        <activity
            android:name=".RandomNumber"
            android:label="@string/title_activity_random_number"
            android:theme="@style/Theme.HW11.NoActionBar"></activity>
        <activity
            android:name=".ImageViewer"
            android:label="@string/title_activity_image_viewer"
            android:theme="@style/Theme.HW11.NoActionBar" />
        <activity
            android:name=".randomNum"
            android:label="@string/title_activity_random_num"
            android:theme="@style/Theme.HW11.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>